import 'package:flutter/material.dart';

class CommonIcons {
  static Info() {
    return const Icon(
      Icons.info_outline,
      size: 35,
      color: Colors.blue,
    );
  }

  static Check() {
    return const Icon(
      Icons.check_circle_outline,
      size: 35,
      color: Colors.blue,
    );
  }

  static Caution() {
    return const Icon(
      Icons.info_outline,
      size: 35,
      color: Colors.deepOrangeAccent,
    );
  }
}
